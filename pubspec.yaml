name: confeito
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  sqflite: ^2.4.2
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  intl: ^0.20.2
  extended_masked_text: ^3.0.1
  mask_text_input_formatter: ^2.9.0
  month_picker_dialog: ^6.7.1
  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
    - assets/images/placeholder_product.jpeg
